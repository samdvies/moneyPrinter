# Adversarial module fixtures for sandbox_runner tests.
# Each file defines: def run(snapshot, params): ...
# These files are intentionally malicious; they are excluded from ruff/mypy
# via the root pyproject.toml extend-exclude / mypy exclude rules.
