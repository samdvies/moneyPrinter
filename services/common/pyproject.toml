[project]
name = "algobet-common"
version = "0.0.1"
requires-python = ">=3.12,<3.13"
description = "Shared utilities and types for algo-betting services"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/algobet_common"]
